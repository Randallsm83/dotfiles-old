# =============================================================================
# MISE CONFIGURATION - WINDOWS SPECIFIC
# =============================================================================
# This file is automatically loaded on Windows systems only.
# On Windows, scoop handles CLI tools (bat, eza, fd, ripgrep, neovim, etc.)
# while mise handles language runtimes (node, python, go, rust, ruby).
# =============================================================================

[tools]
# NOTE: On Windows, CLI tools (bat, eza, fd, ripgrep, starship, neovim, etc.)
# are installed via scoop instead of mise for better Windows integration.
# Only language runtime overrides should be added here if needed.

# Example Windows-specific version override:
# python = "3.11"  # Use older version on Windows if needed
